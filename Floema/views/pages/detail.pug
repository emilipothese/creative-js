extends ../_includes/layout

block variables
  - var template = 'detail'

block content
  .detail
    .detail__wrapper
      figure.detail__media
        img.detail__media__image(alt=product.media.image.alt src=product.media.image.url)

    .detail__information
      p.detail__information__collection=product.data.collection
